import * as zod from "zod";
import { Drawer } from "@medusajs/ui";
import { FormProvider, useForm } from "react-hook-form";
import { PostAdminUpdate{{toPascalCase module.singular}} } from "../../../../api/admin/{{toKebabCase module.plural}}/validators";
import { sdk } from "../../../lib/sdk";
import { useNavigate } from "react-router-dom";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { zodResolver } from "@hookform/resolvers/zod";
import { SelectField } from "../../../components/form/select-field";
import { InputField } from "../../../components/form/input-field";
import { SwitchField } from "../../../components/form/switch-field";
import { FormLayout } from "../../../components/form/form-layout";
import { DrawerFormLayout } from "../../../components/drawer-form-layout";
import { useDrawer } from "../../../components/drawer";
import { {{toPascalCase module.singular}} } from "../../../types";
{{#with (find module.models "name" module.singular)}}
{{#if fields}}
{{#each fields}}
{{#if relation}}
import { List{{toPascalCase relation.model}}sRes } from "../../../types";
{{/if}}
{{/each}}
{{/if}}

const schema = PostAdminUpdate{{toPascalCase ../module.singular}};
type Edit{{toPascalCase ../module.singular}}FormData = zod.infer<typeof schema>;

type {{toPascalCase ../module.singular}}EditProps = {
  {{toCamelCase ../module.singular}}: {
    id: string;
    {{#each fields}}
    {{#if (eq type "text")}}
    {{toSnakeCase name}}: string;
    {{/if}}
    {{#if (eq type "boolean")}}
    {{toSnakeCase name}}: boolean;
    {{/if}}
    {{#if (eq type "number")}}
    {{toSnakeCase name}}: number;
    {{/if}}
    {{#if relation}}
    {{#if (eq relation.type "belongsTo")}}
    {{toSnakeCase name}}: string;
    {{toSnakeCase name}}_id: string;
    {{toSnakeCase name}}?: { id: string; name: string };
    {{/if}}
    {{#if (eq relation.type "hasMany")}}
    {{toSnakeCase name}}: string;
    {{toSnakeCase name}}_ids: string[];
    {{toSnakeCase name}}?: { id: string; name: string }[];
    {{/if}}
    {{#if (eq relation.type "manyToMany")}}
    {{toSnakeCase name}}: string;
    {{toSnakeCase name}}_ids: string[];
    {{toSnakeCase name}}?: { id: string; name: string }[];
    {{/if}}
    {{/if}}
    {{/each}}
  };
  onClose: () => void;
};

function {{toPascalCase ../module.singular}}EditForm({ {{toCamelCase ../module.singular}}, onClose }: {{toPascalCase ../module.singular}}EditProps) {
  const navigate = useNavigate();
  const { close } = useDrawer();
  const queryClient = useQueryClient();
  
  const form = useForm<Edit{{toPascalCase ../module.singular}}FormData>({
    defaultValues: {
      {{#each fields}}
      {{#if (eq type "text")}}
      {{toSnakeCase name}}: {{toCamelCase ../module.singular}}.{{toSnakeCase name}},
      {{/if}}
      {{#if (eq type "boolean")}}
      {{toSnakeCase name}}: {{toCamelCase ../module.singular}}.{{toSnakeCase name}},
      {{/if}}
      {{#if (eq type "number")}}
      {{toSnakeCase name}}: {{toCamelCase ../module.singular}}.{{toSnakeCase name}},
      {{/if}}
      {{#if relation}}
      {{#if (eq relation.type "belongsTo")}}
      {{toSnakeCase name}}: {{toCamelCase ../module.singular}}.{{toSnakeCase name}},
      {{toSnakeCase name}}_id: {{toCamelCase ../module.singular}}.{{toSnakeCase name}}_id,
      {{/if}}
      {{#if (eq relation.type "hasMany")}}
      {{toSnakeCase name}}: {{toCamelCase ../module.singular}}.{{toSnakeCase name}},
      {{toSnakeCase name}}_ids: {{toCamelCase ../module.singular}}.{{toSnakeCase name}}_ids,
      {{/if}}
      {{#if (eq relation.type "manyToMany")}}
      {{toSnakeCase name}}: {{toCamelCase ../module.singular}}.{{toSnakeCase name}},
      {{toSnakeCase name}}_ids: {{toCamelCase ../module.singular}}.{{toSnakeCase name}}_ids,
      {{/if}}
      {{/if}}
      {{/each}}
    },
    resolver: zodResolver(schema),
  });

  {{#if fields}}
  {{#each fields}}
  {{#if relation}}
  const { data: {{toCamelCase relation.model}}Data } = useQuery<List{{toPascalCase relation.model}}sRes>({
    queryKey: ["{{toSnakeCase relation.model}}s"],
    queryFn: () => sdk.client.fetch("/admin/{{toKebabCase ../../module.plural}}/{{toKebabCase relation.model}}s"),
  });

  const {{toCamelCase relation.model}}s = {{toCamelCase relation.model}}Data?.{{toSnakeCase relation.model}}s || [];
  {{/if}}
  {{/each}}
  {{/if}}

  const handleSubmit = form.handleSubmit(async (data) => {
    try {
      await sdk.client.fetch(`/admin/{{toKebabCase ../module.plural}}/{{{propAccess (toCamelCase ../module.singular) "id" templateLiteral=true}}}`, {
        method: "POST",
        body: data,
      });

      // Invalidate and refetch queries
      await queryClient.invalidateQueries({ queryKey: ["{{toSnakeCase ../module.plural}}"] });
      
      // Reset form
      form.reset();
      
      // Close drawer and navigate
      close();
      navigate("/{{toKebabCase ../module.plural}}");
    } catch (error) {
      console.error("Failed to update {{toLowerCase ../module.singular}}:", error);
    }
  });

  return (
    <FormProvider {...form}>
      <DrawerFormLayout
        title="Edit {{toTitleCase ../module.singular}}"
        onSubmit={handleSubmit}
      >
        <FormLayout>
          {{#each fields}}
          {{#if (eq type "text")}}
          <InputField
            name="{{toSnakeCase name}}"
            control={form.control}
            label="{{toTitleCase name}}"
          />
          {{/if}}
          {{#if (eq type "boolean")}}
          <SwitchField
            name="{{toSnakeCase name}}"
            control={form.control}
            label="{{toTitleCase name}}"
          />
          {{/if}}
          {{#if (eq type "number")}}
          <InputField
            name="{{toSnakeCase name}}"
            control={form.control}
            label="{{toTitleCase name}}"
            type="number"
          />
          {{/if}}
          {{#if relation}}
          {{#if (eq relation.type "belongsTo")}}
          <InputField
            name="{{toSnakeCase name}}"
            control={form.control}
            label="{{toTitleCase name}}"
          />
          <SelectField
            name="{{toSnakeCase name}}_id"
            control={form.control}
            label="{{toTitleCase name}}"
            placeholder="Select {{toLowerCase name}}..."
            options={ {{toCamelCase relation.model}}s }
          />
          {{/if}}
          {{#if (eq relation.type "hasMany")}}
          <InputField
            name="{{toSnakeCase name}}"
            control={form.control}
            label="{{toTitleCase name}}"
          />
          <SelectField
            name="{{toSnakeCase name}}_ids"
            control={form.control}
            label="{{toTitleCase name}}"
            placeholder="Select {{toLowerCase name}}..."
            options={ {{toCamelCase relation.model}}s }
            isMulti
          />
          {{/if}}
          {{#if (eq relation.type "manyToMany")}}
          <InputField
            name="{{toSnakeCase name}}"
            control={form.control}
            label="{{toTitleCase name}}"
          />
          <SelectField
            name="{{toSnakeCase name}}_ids"
            control={form.control}
            label="{{toTitleCase name}}"
            placeholder="Select {{toLowerCase name}}..."
            options={ {{toCamelCase relation.model}}s }
            isMulti
          />
          {{/if}}
          {{/if}}
          {{/each}}
        </FormLayout>
      </DrawerFormLayout>
    </FormProvider>
  );
}

export function {{toPascalCase ../module.singular}}Edit(props: {{toPascalCase ../module.singular}}EditProps) {
  return (
    <Drawer.Content aria-describedby="edit-{{toKebabCase ../module.singular}}-description">
      <{{toPascalCase ../module.singular}}EditForm {...props} />
    </Drawer.Content>
  );
}
{{/with}}