import { MedusaRequest, MedusaResponse } from "@medusajs/framework/http";
import { z } from "zod";
import { update{{toPascalCase model.name}}Workflow } from "../../../../../workflows/update-{{toKebabCase model.name}}";
import { PostAdminUpdate{{toPascalCase model.name}} } from "../validators";

type PostAdminUpdate{{toPascalCase model.name}}Type = z.infer<
  typeof PostAdminUpdate{{toPascalCase model.name}}
>;
//asdfsdfa
export const POST = async (
  req: MedusaRequest<PostAdminUpdate{{toPascalCase model.name}}Type>,
  res: MedusaResponse
) => {
  const { result } = await update{{toPascalCase model.name}}Workflow(req.scope).run({
    input: {
      id: req.params.id,
      ...req.validatedBody,
    },
  });

  res.json({ {{toSnakeCase model.name}}: result });
};