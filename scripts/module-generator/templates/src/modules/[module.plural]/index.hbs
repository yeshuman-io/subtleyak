import {{toPascalCase module.moduleName}}Service from "./service";
import { Module } from "@medusajs/framework/utils";

// Single module for all {{module.moduleName}}-related models
export const {{toUpperCase module.moduleName}}_MODULE = "{{module.moduleName}}";

// Export the {{module.moduleName}} service that handles all models
export default Module({{toUpperCase module.moduleName}}_MODULE, {
  service: {{toPascalCase module.moduleName}}Service
});