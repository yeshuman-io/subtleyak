import { MedusaService } from "@medusajs/framework/utils";
import  from "./";
{{#each module.models}}
import {{toPascalCase name}} from "./models/{{name}}";
{{/each}}

class Service extends MedusaService({
  ,
  {{#each module.models}}{{toPascalCase name}}{{#unless @last}},{{/unless}}{{/each}}
}) {}

export default Service; 